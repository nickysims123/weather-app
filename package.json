{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "This is my take on the traditional \"weather app\" project. It will use information from visualcrossing's free weather API, so don't mind the publicly exposed API key. That will be handled in the backend later.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "webpack-cli": "^5.1.4"
  }
}
